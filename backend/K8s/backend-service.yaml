apiVersion: v1
kind: Service
metadata:
  name: proj-backend-service
  namespace: proj
spec:
  selector:
    app: proj-backend
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
      nodePort: 31156
  type: NodePort